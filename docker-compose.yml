services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "${FRONTEND_PORT:-4200}:4200"
